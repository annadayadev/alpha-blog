<!-- here we call back or render the code that we moved into the _errors.html.erb, so we can use it. 

here we added obj: @article because on the errors.html.erb we changed @articles into obj to make it more generic so we can use globally for example the users which there will be no @articles but then because we changed it into obj we can now use our error files as shared or globally.

so here in our articles we can say obj: @article, if its for users we can say obj: @users. 
-->

<%= render 'shared/errors', obj: @article %>

<div class='row'>
    <div class='col-xs-12'>

<!--for the (f) you can choose other name-->
 <!-- => means hashrocket, by using these code form-horizontal proper alignment of our form has been added. It looks good now -->
        <%= form_for(@article, :html => {class: "form-horizontal", role: "form"}) do |f| %> 
        <div class="form-group">
                <div class="control-label col-sm-2">
                    
                    <%= f.label :title %><br />
                
                </div>
                <div class="col-sm-8">
                    
                    <%= f.text_field :title, class: "form-control", placeholder: "Title of Article", autofocus: true %>
            
                </div>
            </div>
            
            <div class="form-group">
                <div class="control-label col-sm-2">
                <%= f.label :description %>
                
            </div>
            <div class="col-sm-8">
                <%= f.text_area :description, rows: 10, class: "form-control", placeholder: "Body of the article", autofocus: true %>
            </div>
        </div>
            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <%= f.submit class: 'btn btn-primary btn-lg' %>
                </div>
            </div>
        <% end %>
        
        <div class="col-xs-4 col-xs-offset-4"></div>
           [ <%= link_to "Cancel request and return to articles listing", articles_path %> ]
        </div>
        
    </div>
</div>




















