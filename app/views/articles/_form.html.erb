

<% if @article.errors.any? %>
<h2>The following errors prevented the article from getting created</h2>
<ul>
    <!-- so, for each full_message do something, ->
    <% @article.errors.full_messages.each do |msg| %>
    
    
    <!--and what do we want to do with the msg, we simply want it to display, but we can have more than 1 message, we can have more than 1 error, so we simply use <li>-->

        <li><%= msg %></li>

    <% end %>
</ul>
<% end %>

<div class='row'>
    <div class='col-xs-12'>

<!--for the (f) you can choose other name-->
 <!-- => means hashrocket, by using these code form-horizontal proper alignment of our form has been added. It looks good now -->
        <%= form_for(@article, :html => {class: "form-horizontal", role: "form"}) do |f| %> 
        <div class="form-group">
                <div class="control-label col-sm-2">
                    
                    <%= f.label :title %><br />
                
                </div>
                <div class="col-sm-8">
                    
                    <%= f.text_field :title, class: "form-control", placeholder: "Title of Article", autofocus: true %>
            
                </div>
            </div>
            
            <div class="form-group">
                <div class="control-label col-sm-2">
                <%= f.label :description %>
                
            </div>
            <div class="col-sm-8">
                <%= f.text_area :description, rows: 10, class: "form-control", placeholder: "Body of the article", autofocus: true %>
            </div>
        </div>
            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <%= f.submit class: 'btn btn-primary btn-lg' %>
                </div>
            </div>
        <% end %>
        
        <div class="col-xs-4 col-xs-offset-4"></div>
           [ <%= link_to "Cancel request and return to articles listing", articles_path %> ]
        </div>
        
    </div>
</div>




















